# office360 — web

SvelteKit 2 frontend. It talks to the Go API at `PUBLIC_API_URL` (see the root `.env`).

Run it from the repo root with `./make.ps1 web`, or here with `npm run dev`.
Type-check with `npm run check`.

Setup, architecture, and API reference live in the [root README](../README.md).
